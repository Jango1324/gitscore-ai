# GitHub personal access token (optional but strongly recommended).
# Unauthenticated GitHub API requests are capped at 60/hour; an
# authenticated token raises that to 5000/hour. No special scopes are
# required for reading public profile/repository data.
GITHUB_TOKEN=
